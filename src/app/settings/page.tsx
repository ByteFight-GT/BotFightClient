"use client";

import React from 'react';

type MatchPlayerPageProps = {
	
};

const SettingsPage = (props: MatchPlayerPageProps) => {
	return (
		<div>
			<h1>Settings</h1>
		</div>
	);
};

export default SettingsPage;
